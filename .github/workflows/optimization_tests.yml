name: Optimization Tests CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # Manual trigger

jobs:
  # SUCCESS CASE: Normal tests
  test-success:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run tests (SUCCESS mode)
      run: |
        pytest test_minimal.py -v -s
  
  # FAILURE CASE: Demonstrate failure (optional - uncomment to enable)
  # test-failure-demo:
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v3
  #   
  #   - name: Set up Python
  #     uses: actions/setup-python@v4
  #     with:
  #       python-version: '3.10'
  #   
  #   - name: Install dependencies
  #     run: |
  #       python -m pip install --upgrade pip
  #       pip install -r requirements.txt
  #   
  #   - name: Run tests (FAILURE mode - unrealistic precision)
  #     env:
  #       CI_DEMO_FAILURE: "1"
  #     run: |
  #       pytest test_minimal.py -v -s
